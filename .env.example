# Grid-X root env (copy to .env). Component-specific env: see coordinator/env.example and worker/env.example.

# When running worker on a different machine, set there:
# COORDINATOR_WS=ws://<COORDINATOR_IP_OR_HOST>:8080/ws/worker
# WORKER_OWNER_ID=your_user_id

# Coordinator (when run at root or in docker-compose)
# GRIDX_HTTP_PORT=8081
# GRIDX_WS_PORT=8080
# GRIDX_DB_PATH=gridx.db
