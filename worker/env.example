# Grid-X Worker - set when coordinator is on ANOTHER machine
# Copy to .env or export before running.

# Required when coordinator is remote (use coordinator host IP or hostname)
COORDINATOR_WS=ws://<COORDINATOR_HOST>:8080/ws/worker
# Example: COORDINATOR_WS=ws://192.168.1.10:8080/ws/worker

# Optional: user_id that receives credits when this worker runs jobs
WORKER_OWNER_ID=

# Optional: Docker socket (default: Linux /var/run/docker.sock, Windows npipe)
# GRIDX_DOCKER_SOCKET=unix:///var/run/docker.sock
# DOCKER_HOST=unix:///var/run/docker.sock
